services:
  postgres:
    image: postgres:latest
    container_name: dbetter_traincompositions-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql
  rabbitmq:
    image: rabbitmq:4-management
    container_name: dbetter_traincompositions-rabbitmq
    restart: unless-stopped
    ports:
      - "15672:15672"
      - "5672:5672"

volumes:
  postgres_data:
